import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export const ShadowingPracticeScreen = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
};
